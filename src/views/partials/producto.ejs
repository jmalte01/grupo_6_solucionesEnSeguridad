<body id="producto">
    <div class="flex-item">                
        <figure> <img src="/img/<%= producto.image %>" /></figure>
    </div>
    <div class="flex-item">
        <!--
            tomamos el producto que nos envia el response de la ruta /producDetail/:id
            y lo renderizamos accediendo a sus propiedades
        -->

        <div class="detalles">
            <p><%= producto.category.name %></p>
            <h2><%= producto.productName %></h2>
            <% if (producto.detail) { %>
                <p>
                    <%= producto.detail %>
                </p>
            <% } else { %>
                <p class="descripcion"> <%= producto.description %></p>
            <% } %>
            <br />
            <p class="precio"><b>$ <%= producto.price %>.-</b></p>
            <br />
            <button class="agregar">Agregar al carrito</button>
        </div>
    </div>    
</body>
